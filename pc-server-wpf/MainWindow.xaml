<Window x:Class="SyncLinkServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="NexusLink Desktop" Height="800" Width="1200" Icon="Logo.png"
        Background="#F8F9FA" FontFamily="Inter, Segoe UI" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Reusable styles based on the Tailwind design -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#424753" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="Margin" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E1E3E4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarButtonActiveStyle" TargetType="Button" BasedOn="{StaticResource SidebarButtonStyle}">
            <Setter Property="Background" Value="#D6E3FB" />
            <Setter Property="Foreground" Value="#0F1C2D" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#F3F4F5" BorderBrush="#E1E3E4" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="24,32,24,24">
                    <TextBlock Text="NexusLink Desktop" FontSize="18" FontWeight="Bold" Foreground="#00459A" VerticalAlignment="Center"/>
                </StackPanel>



                <!-- Navigation -->
                <StackPanel Grid.Row="2" Margin="0,8,0,0">
                    <Button Style="{StaticResource SidebarButtonActiveStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ViewDashboardOutline" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SidebarButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="BellOutline" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Notifications" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SidebarButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FolderOutline" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Files" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SidebarButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CogOutline" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Settings" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Background="#00459A" Foreground="White" BorderThickness="0" Margin="24,32,24,0" Padding="12" materialDesign:ButtonAssist.CornerRadius="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Add New Device" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </Button>
                </StackPanel>


            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E1E3E4" BorderThickness="0,0,0,1">
                <Grid Margin="24,0">
                    <TextBlock Text="NexusLink" FontSize="20" FontWeight="Black" Foreground="#00459A" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    

                </Grid>
            </Border>

            <!-- Dashboard Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="32">
                    <TextBlock Text="Device Dashboard" FontSize="28" FontWeight="Bold" Foreground="#191C1D" Margin="0,0,0,24"/>

                    <!-- Bento Grid -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Primary Card -->
                        <Border Grid.Column="0" Grid.Row="0" Background="#FFFFFF" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="16" Margin="0,0,24,24" Padding="24">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <Grid Grid.Row="0">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Ellipse Width="8" Height="8" Fill="#00459A" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="ACTIVE CONNECTION" FontSize="12" FontWeight="SemiBold" Foreground="#00459A"/>
                                        </StackPanel>
                                        <TextBlock x:Name="TxtDeviceName" Text="Waiting for Device..." FontSize="36" FontWeight="Bold" Foreground="#191C1D"/>
                                        <TextBlock Text="Last synced: Just now" FontSize="14" Foreground="#424753" Margin="0,4,0,0"/>
                                    </StackPanel>
                                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="#EDEEEF" CornerRadius="30" Padding="12">
                                        <materialDesign:PackIcon x:Name="IconDevice" Kind="Smartphone" Width="36" Height="36" Foreground="#00459A"/>
                                    </Border>
                                </Grid>

                                <Grid Grid.Row="2" Margin="0,32,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Background="#F3F4F5" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,12,0">
                                        <StackPanel>
                                            <TextBlock Text="Storage" FontSize="12" Foreground="#424753"/>
                                            <TextBlock x:Name="TxtStorageFree" Text="-- Free" FontSize="18" FontWeight="SemiBold" Foreground="#191C1D" Margin="0,4,0,8"/>
                                            <ProgressBar x:Name="ProgStorage" Value="0" Height="6" materialDesign:TransitionAssist.DisableTransitions="True" Foreground="#00459A" Background="#E1E3E4" BorderThickness="0"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Background="#F3F4F5" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="8" Padding="12" Margin="6,0,6,0">
                                        <StackPanel>
                                            <TextBlock Text="Network" FontSize="12" Foreground="#424753"/>
                                            <TextBlock x:Name="TxtNetworkName" Text="Checking..." FontSize="14" FontWeight="SemiBold" Foreground="#191C1D" Margin="0,4,0,4" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="Connected" FontSize="12" Foreground="#00459A"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="2" Background="#F3F4F5" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="8" Padding="12" Margin="12,0,0,0">
                                        <StackPanel>
                                            <TextBlock Text="Sync Speed" FontSize="12" Foreground="#424753"/>
                                            <TextBlock x:Name="TxtSyncSpeed" Text="0 KB/s" FontSize="18" FontWeight="SemiBold" Foreground="#191C1D" Margin="0,4,0,4"/>
                                            <TextBlock Text="Network IO" FontSize="12" Foreground="#727785"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>

                        <!-- Battery Card -->
                        <Border Grid.Column="1" Grid.Row="0" Background="#FFFFFF" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="16" Margin="0,0,0,24" Padding="24">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="Battery Level" FontSize="18" FontWeight="SemiBold" Foreground="#191C1D" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                                
                                <Grid Width="140" Height="140" HorizontalAlignment="Center" Margin="0,16,0,16">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <!-- Custom Battery Icon (XAML) -->
                                        <Grid Width="68" Height="32" HorizontalAlignment="Center" Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="4"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Battery Body -->
                                            <Border Grid.Column="0" BorderBrush="#424753" BorderThickness="2" CornerRadius="4" Padding="2" Background="Transparent">
                                                <Border x:Name="BatteryFill" Background="#00459A" CornerRadius="2" HorizontalAlignment="Left" Width="0"/>
                                            </Border>
                                            
                                            <!-- Battery Terminal -->
                                            <Border Grid.Column="1" Background="#424753" CornerRadius="0,2,2,0" Height="12" Margin="0,10,0,10"/>
                                        </Grid>
                                        <TextBlock x:Name="TxtBatteryLvl" Text="--" FontSize="32" FontWeight="Bold" Foreground="#191C1D" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock x:Name="TxtBatteryState" Text="Waiting for data..." FontSize="14" Foreground="#424753" HorizontalAlignment="Center"/>
                                <TextBlock Text="Approx. 20m until full" FontSize="12" Foreground="#727785" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Actions -->
                        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="BtnOpenGallery" MouseLeftButtonDown="BtnOpenGallery_Click" Cursor="Hand" Grid.Column="0" Background="#FFFFFF" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,12,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#E6F0FF" CornerRadius="12" Padding="12">
                                        <materialDesign:PackIcon Kind="ImageMultipleOutline" Width="32" Height="32" Foreground="#00459A"/>
                                    </Border>
                                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="Browse and Sync File" FontSize="18" FontWeight="SemiBold" Foreground="#191C1D"/>
                                        <TextBlock Text="Browse and send files to connected phone" FontSize="14" Foreground="#424753" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border x:Name="BtnMirrorScreen" MouseLeftButtonDown="BtnMirrorScreen_Click" Cursor="Hand" Grid.Column="1" Background="#FFFFFF" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="16" Padding="20" Margin="12,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#F0F4FF" CornerRadius="12" Padding="12">
                                        <materialDesign:PackIcon Kind="Cast" Width="32" Height="32" Foreground="#525F73"/>
                                    </Border>
                                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="Mirror Screen" FontSize="18" FontWeight="SemiBold" Foreground="#191C1D"/>
                                        <TextBlock Text="Control your device from desktop" FontSize="14" Foreground="#424753" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Recent Activity -->
                        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="#FFFFFF" BorderBrush="#E1E3E4" BorderThickness="1" CornerRadius="16" Padding="24">
                            <StackPanel>
                                <Grid Margin="0,0,0,16">
                                    <TextBlock Text="Recent Activity" FontSize="18" FontWeight="SemiBold" Foreground="#191C1D" VerticalAlignment="Center"/>
                                    <Button Content="View All" Foreground="#00459A" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0"/>
                                </Grid>
                                
                                <Separator Background="#E1E3E4" Margin="-24,0,-24,12"/>

                                <!-- Log items -->
                                <StackPanel x:Name="ListActivity">
                                    <!-- Items added dynamically in C# -->
                                    <TextBlock Text="No activity yet." Foreground="#727785" FontStyle="Italic" Margin="8,8,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
